import styled from 'styled-components'

const Title = styled.span`
    font-size: 0.7em;
    margin: 10px 0px 5px;
    width: 100%;
    text-align: left;
    font-weight: bold;
`

export default Title